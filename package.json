{
  "name": "defiverse-smart-contracts",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "develop": "truffle develop",
    "compile": "truffle compile",
    "deploy": "truffle migrate --network defiverse-testnet --reset --f 1 --to 1",
    "deploy:defiverse:testnet": "truffle migrate --network defiverse-testnet --reset --f 1 --to 1",
    "deploy:defiverse:mainnet": "truffle migrate --network defiverse-mainnet --reset --f 1 --to 1",
    "deploy:usdc:testnet": "truffle migrate --network defiverse-testnet --reset --f 3 --to 3",
    "deploy:usdc:mainnet": "truffle migrate --network defiverse-mainnet --reset --f 3 --to 3",
    "deploy:token:testnet": "truffle migrate --network token-testnet --reset --f 6 --to 6",
    "deploy:token:mainnet": "truffle migrate --network token-mainnet --reset --f 6 --to 6",
    "deploy:oracle:sandverse": "truffle migrate --network oracle-sandverse --reset --f 4 --to 4",
    "setup:oracle:sandverse": "truffle migrate --network oracle-sandverse --compile-none --reset --f 5 --to 5",
    "test": "npx hardhat test"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "hungvu",
  "license": "MIT",
  "devDependencies": {
    "ethereum-waffle": "^3.4.4",
    "ethereumjs-abi": "^0.6.8",
    "ethers": "^5.6.7",
    "hardhat-gas-reporter": "^1.0.9",
    "inquirer": "^8.2.4",
    "lite-server": "^2.6.1",
    "prettier": "^2.6.2",
    "prettier-plugin-solidity": "^1.0.0-beta.13",
    "solhint": "^3.3.7",
    "solidity-coverage": "^0.7.0"
  },
  "dependencies": {
    "@evmchain/contract-deployer": "^1.3.5",
    "@openzeppelin/contracts": "^4.8.0",
    "@openzeppelin/contracts-upgradeable": "^4.1.0",
    "@truffle/hdwallet-provider": "2.1.2",
    "dotenv": "^8.2.0",
    "truffle": "5.6.8",
    "truffle-flattener": "^1.5.0",
    "web3": "^1.7.3"
  }
}
